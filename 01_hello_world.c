/*
Hello, this is the first code we will work on the block between "slash + asterisk" and
"asterisk + slash" is called a comment.

The comments will be ignored by the compiler, however they are a good way to take note for us, humans!
*/

#include <stdio.h>

int main() {

    //print "hello world to screen"
    printf("Hello world!\n");

    return 0;
} 