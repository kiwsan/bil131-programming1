// Loops

#include <stdio.h>

int main () {

    int i = 10; // always initialize the variable!

    while (i > 0) {
        printf("This is line #%d and i is greater then zero\n", i);
        i--;
    }

    return 0;
}